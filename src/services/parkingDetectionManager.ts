/**
 * Parking detection — pure helpers (nearest bay, distances, auto-release heuristic).
 * Live location loop and notifications: `ParkingDetectionContext`.
 */

export * from './parkingDetectionCore';
